name: outfit_maker_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  # Imagenes
  image_picker: ^1.0.7

  # HTTP y estado
  http: ^1.2.0

  # Utilidades para rutas de archivos
  path: ^1.9.0
  path_provider: ^2.1.1

  # Almacenamiento local
  shared_preferences: ^2.2.2

  # Calendario
  table_calendar: ^3.0.9

  # Iconos de iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
